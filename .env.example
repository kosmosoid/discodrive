# Copy to .env and fill in. .env is NOT committed to git.

# === Infrastructure secrets — set at deploy time ===
# Postgres connection (host `postgres` = the docker-compose service name).
DATABASE_URL=postgres://disco:disco@postgres:5432/discodrive?sslmode=disable
# Session/JWT signing key (at least 32 random bytes). The server will NOT start with this
# placeholder. Generate with:  openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-secret
# Service base domain.
BASE_DOMAIN=localhost
# Encryption key for secret settings in the DB (AES-256-GCM, EXACTLY 32 bytes). Do NOT keep
# it in the same backup as the DB. The server will NOT start with this placeholder. Generate with:
#   openssl rand -hex 16
SETTINGS_ENCRYPTION_KEY=change-me-to-a-32-byte-secret!!!

# === Postgres container settings (docker-compose) ===
POSTGRES_USER=disco
POSTGRES_PASSWORD=disco
POSTGRES_DB=discodrive

# === Misc ===
# Timezone for all containers (postgres, app, nginx). IANA name, e.g. Europe/Berlin.
# Defaults to UTC when unset.
TZ=UTC
APP_PORT=8080
# Data root inside the container (mirrors users' file trees).
STORAGE_ROOT=/data
# Background jobs: how many versions to keep, trash retention, rescan interval.
# VERSION_KEEP is how many PAST revisions of a file to keep. The current content is the
# file itself and never costs extra — a file that is uploaded and never edited has no
# history at all. 0 turns versioning off completely: overwrites replace the file with no
# way back, and any history already stored is deleted by the trim job.
VERSION_KEEP=10
TRASH_DAYS=30
RESCAN_SECONDS=30
# File serving: true — via nginx (X-Accel-Redirect, zero-copy); false — Go streams it itself
# (for deploys without nginx). Behind nginx, keep this true.
XACCEL_ENABLED=true
# Storage limits.
# STORAGE_TOTAL_GB — how much of the disk discodrive may occupy in total, in binary GB
# (0 = the whole disk). Set it when the disk is shared with anything else: on a 3 TB disk,
# STORAGE_TOTAL_GB=1024 means the admin panel refuses to hand out more than 1 TB of user
# quotas in total, AND no write is accepted once all users together hold 1 TB — so the
# other 2 TB stay untouched. Occupied space counts live files, files still in the trash
# (kept TRASH_DAYS), version snapshots (kept VERSION_KEEP) and downloaded podcast
# episodes: everything actually on disk. An uploaded file costs its own size; only
# overwriting it starts a history, and each past revision then costs its size too.
# Lowering it below what is already stored blocks new writes; it never deletes anything.
STORAGE_TOTAL_GB=0
# DEFAULT_USER_QUOTA_GB — quota given to a newly created user when the admin leaves the
# quota field empty (0 = no personal quota, only STORAGE_TOTAL_GB applies). Admins are
# created without a quota either way. Existing users are never changed by this.
DEFAULT_USER_QUOTA_GB=0

# Saved items: max size of a single server-side download in MB (0 = unlimited).
# 32 GB by default — ISOs and big archives are the point; lower it if disk is tight.
SAVED_MAX_DOWNLOAD_MB=32768
